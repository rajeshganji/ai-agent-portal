<!DOCTYPE html>
<html>
<head>
    <title>Agent Toolbar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <span class="navbar-brand">Welcome, <%= agent.agentName %></span>
            <div class="d-flex">
                <select id="statusSelect" class="form-select me-2">
                    <option value="AVAILABLE" <%= agent.agentStatus === 'AVAILABLE' ? 'selected' : '' %>>Available</option>
                    <option value="BUSY" <%= agent.agentStatus === 'BUSY' ? 'selected' : '' %>>Busy</option>
                    <option value="AWAY" <%= agent.agentStatus === 'AWAY' ? 'selected' : '' %>>Away</option>
                </select>
                <button id="logoutBtn" class="btn btn-outline-danger">Logout</button>
            </div>
        </div>
    </nav>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/js/toolbar.js"></script>
</body>
</html>